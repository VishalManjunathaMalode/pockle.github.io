name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run a sample script
        run: echo "Hello from GitHub Actions!"

      - name: Serve static site (optional)
        run: |
          # You can add steps to deploy or test your static site here